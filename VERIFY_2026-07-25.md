# Verification — NS-2.35+nOBS/BHP reconstruction

Date: 2026-07-25 (Asia/Saigon)

## Decision requested

Approve the implementation and evidence package as a verified simulation/reconstruction scaffold. Do not approve the unreproduced thesis-effectiveness numbers as reproduced findings.

## Changes verified

1. OpenClaw runtime configuration
   - Backed up `/home/hitokiri/.openclaw/openclaw.json` to `/home/hitokiri/.openclaw/openclaw.json.bak-runtime-20260725-0238`.
   - Added `agents.defaults.timeoutSeconds=172800`.
   - JSON validation passed; `openclaw status` reports the local loopback Gateway reachable and running.

2. Forged-BHP lifecycle
   - Current `op-sragent.cc` contains the control-only branch that frees the phantom and control packet and returns before forwarding into the normal control/data lifecycle.
   - `bhp_control_scenario.tcl` declares `bhp_ingress` global in `finish`, removing the prior Tcl variable error.

3. Scheduler hardening
   - Initialized `SchList* temp` and `SchList* besttemp` to null in `op-schedule.cc` to prevent undefined state on the no-candidate/reject path.

4. Build
   - `make -j2 ns` passed.
   - Binary: `nobs/build/ns-allinone-2.35/ns-2.35/ns`
   - SHA-256: `e2ff7127706c4e5f01891fc1915f013f3dd1a083fdec32252b4428ba94a76ba6`

## Runtime verification

### Forged-BHP pilot

- Fresh run: `rc=0`
- `stderr`: empty
- `RUN_COMPLETE`: present
- Guard counters: `admitted=0 dropped=200 quarantined=199`
- This validates the reject/quarantine control path and crash-free lifecycle for this profile.

### Forged-BHP admitted-path pilot

- Fresh run with the higher reservation budget: `rc=0`
- `stderr`: empty
- Guard counters: `admitted=2 dropped=198 quarantined=197`
- Audit events: `observe=200 detect=2 act=200`
- This verifies that the admitted forged-BHP path also completes without the previous SIGSEGV; the remaining controls are then limited/quarantined by the configured guard policy.

### Native S0/S1/S2 pilot matrix

All four fresh runs returned `rc=0` with empty stderr:

| Scenario | Legal TCP packets | Legal TCP bytes | Offered optical bursts |
|---|---:|---:|---:|
| S0 / none | 98 | 99,920 | 42 |
| S1 / none | 98 | 99,920 | 341 |
| S2 / rate_limit | 98 | 99,920 | 143 |
| S2 / isolation | 98 | 99,920 | 193 |

The attacker block is active in `scenario.tcl`. Attack/mitigation changes offered optical load, but this 0.6 s configuration does not change legal TCP delivery.

### Eight-seed matrix

- 32/32 runs completed and parsed: 8 seeds × 4 scenarios.
- All seeds are deterministic under current `CBR random_=0` configuration:
  - S0: legal mean 98 packets; offered mean 42 bursts.
  - S1: legal mean 98 packets; offered mean 341 bursts.
  - S2 rate-limit: legal mean 98 packets; offered mean 143 bursts.
  - S2 isolation: legal mean 98 packets; offered mean 193 bursts.

### Regression tests

- `14/14 PASS`
- Includes trace parser, pairing/drop semantics, real nOBS smoke test, CLI validation, topology/trace configuration, and mitigation configuration.

## Claim gate

PASS:
- NS-2.35+nOBS builds and runs.
- Native optical attack load enters the real nOBS path.
- Forged-BHP reject/quarantine pilot completes without crash.
- Rate limiting and application-stop isolation reduce offered attack bursts.
- Trace parser and scenario regression tests pass.

NOT REPRODUCED:
- Thesis values 82,568 / 38,281 / 52,244 / 93,238 packets.
- Legal-throughput degradation in S1.
- Legal-throughput recovery in S2.
- Production-ready detector-driven closed loop.

The evidence therefore supports a verified reconstruction/integration scaffold, not confirmation of the thesis-effectiveness numbers or deployment readiness.

## Evidence locations

- Build log: `nobs/logs/rebuild_runtime_20260725.log`
- Unit tests: `nobs/logs/unittest_runtime_20260725.log`
- Forged-BHP reject run: `nobs/experiments/bhp_verified_runtime_20260725/`
- Forged-BHP admitted-path run: `nobs/experiments/bhp_allow_verified_20260725/`
- Four-scenario pilots: `nobs/experiments/pilots/raw/`
- Eight-seed matrix: `nobs/experiments/matrix_8seed_20260725/`
- Utility audit: `nobs/experiments/PRACTICAL_UTILITY_GATE.md`
- Claim matrix: `nobs/experiments/CLAIM_EVIDENCE_UTILITY_MATRIX.csv`
